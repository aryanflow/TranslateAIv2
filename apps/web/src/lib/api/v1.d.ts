/**
 * Regenerated with: pnpm --filter web run generate:api (api must serve /api/docs-json).
 * Placeholder keeps `openapi-fetch` typed until the first codegen run.
 */
export type paths = Record<string, never>;
export type operations = Record<string, never>;
export type components = Record<string, never>;
