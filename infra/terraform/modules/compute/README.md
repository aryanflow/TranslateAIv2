# compute (stub)

Run containers from [`docker/Dockerfile.api`](../../../../docker/Dockerfile.api) and [`docker/Dockerfile.web`](../../../../docker/Dockerfile.web): **ECS/Fargate**, **Cloud Run**, **AKS**, **App Service**, etc.

Pass env from database/cache/object_storage modules and secrets manager.
