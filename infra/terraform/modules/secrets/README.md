# secrets (stub)

Store **LANGDOCK_BEARER_TOKEN**, DB passwords, and signing keys in **AWS Secrets Manager**, **GCP Secret Manager**, **Azure Key Vault**, or **Vault**.

Inject at runtime into API/worker tasks — never bake into images.
